#include <stdlib>
#include <conio>

class shape()
{
      public:
             shape(void);
             `shape(void);
             void rotate_left();
             void rotate_right();
             void getshape(block);
};
